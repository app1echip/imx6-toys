tiny http file server

usage:

./httpd [<directory>] [-p <port>]

  [<directory>]  The directory to share.
                 (default: current working directory)
  [-p <port>]    Port number server listens to.
                 (default: 8080)
